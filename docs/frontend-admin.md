# Admin Tools: the entity-driven Workbench

The admin tools (`src/routes/admin`) are a single config-driven **Workbench** (`src/routes/admin/workbench`): a master–detail page with a searchable record list and a tabbed detail panel, under one catalogue-wide "Save Changes" bar. It replaced a separate page per operation (which forced re-selecting the same entity in each). Split out of [frontend.md](./frontend.md) — this is only needed when working on admin tooling.

- **One component, many entities.** A single `Workbench.svelte` renders any entity described by an `EntityConfig` (`workbench/entities/*.ts`) — list metadata plus an ordered list of typed **sections** (`fields`/`table`/`chips`/`tags`/`usage`/`challenge-condition`/`challenge-reward`). Adding an admin entity is a new config object, not new UI. When a section needs logic the generic kinds don't cover it gets its own `kind` (rendered by `SectionRenderer`) rather than forking the Workbench, kept generic via two opt-in hooks (a one-line `headline` preview and a `dirtyKeys` declaration so a custom section still shows the unsaved dot). A **nameless** entity (e.g. a skill-synthesis recipe, identified by its result skill rather than a stored name) supplies a live `title(rec)` hook that drives the list/detail title and search in place of `name`.
- **Diffing store.** `EntityStore` keeps an editable copy plus a baseline and derives per-record status (added/modified/deleted/clean) by structural comparison — the same dirty-tracking model as the generic `TableEditor`, generalized to whole records. The save bar shows live counts; Discard reverts to baseline.
- **Validation lives where it occurs.** A field is `required` or a non-field section carries a `warn(record)` predicate; the list row, the offending tab, and the field all surface the same warning. The amber **dot** is reserved for unsaved/dirty state and the amber **triangle** for a validation warning — the two never overlap.
- **Save orchestration.** "Save all" commits every pending change. Because the backend persists each relationship through its own endpoint, the save first posts the identity-level Add/Edit/Delete batch, refetches to resolve the ids of newly-added records (the backend appends adds in send order, so the k-th add maps to the k-th new id), then runs each section's child-saver against the resolved id. Each save calls only the endpoints it needs — a record is `modified` when any part differs from baseline, so the identity Edit is sent only when the primary DTO changed and each child-saver no-ops when its collection is unchanged.
- **Reference reads go over the socket; only writes use HTTP.** The catalogues and select-option data load via the same `Get*` socket commands the loading screen uses (through `fetchSocketData`, which throws on a socket error so failures surface as toasts); the persistence calls stay on HTTP. Freshness after an edit is server-side: every admin write reloads the in-memory reference caches, so the post-save refetch serves fresh data. The only reference data still on HTTP is what has no socket command yet (tags).
- **Attribute selection uses a searchable, group-by-type picker, not a flat `<select>`.** The `EAttribute` enum is large (the damage-type amplification/resistance family pushed it past what a native dropdown handles), so every attribute picker is a `field`/`column` of `type: 'attribute'` — it reuses the same `options` provider as `select` but renders the reusable `AttributePicker` (a `Popover`-hosted searchable list grouped by the reference display taxonomy, with the Affinity family split per damage type, mirroring the attribute-breakdown grouping). Add a new attribute picker as `type: 'attribute'`, never `select`.
- **Retired records are excluded from authoring pickers, but never silently dropped.** A retired reference record (see `backend.md` → _Retiring reference data_) stays resolvable by id but is out of circulation, so the select-option providers **omit it from new authoring — a hard block, not a warning**. The one exception is the value a row already references: a retired *current* value is kept in the list with a "retired" affordance, and once the author changes away from it it can't be re-picked. Index-based display lookups stay unfiltered so a retired reference always renders its name.
- **Retiring a referenced record first surfaces a "referenced by" confirm dialog.** The referenced-by surface (boss / unlock-gate / challenge-target / reward / spawn-table memberships) is computed client-side from the cached reference sets (`references.ts`), so retire needs no backend round-trip; an unreferenced record retires without a prompt. It is **advisory only** — retirement never breaks a reference (the id still resolves), so the dialog just lets the admin re-point a gate/boss first. The single genuine integrity block (a live zone's last spawnable enemy) is enforced separately on the backend, not here.
- **Not everything is a Workbench entity — Ops tools live alongside it.** Operations/diagnostics surfaces (the player write-behind dead-letter queue, the socket command dead-letter queue, and the read-only **Content Health** view surfacing the whole-graph content lint; `admin/ops`) are a different *kind* of tool: no catalogue, no save bar, just inspect/act over a live backend resource. They get their own sidebar group (`nav.ts` appends the **Ops** group + tools to the Workbench-derived nav) and `+page.svelte` branches on the active tool key to render their own view instead of `Workbench.svelte`. Such a view owns its state in a reactive controller class (`*.svelte.ts`), reads/writes over HTTP via `ApiRequest`, and guards mutating actions behind `confirmModal`. When adding an Ops tool, follow this split rather than bending it into an `EntityConfig`.
- **A bespoke surface can still live inside a Workbench group.** The progression editor (`workbench/progression`, the **Paths** tool in the Progression group) authors *two* cross-referenced reference catalogues — paths and their proficiency tiers — through one master→detail with a path→tier drill-down, so it can't be a single `EntityConfig`. Like an Ops tool it branches in `+page.svelte` and owns a reactive controller (`ProgressionStore`), but it stays a Workbench surface: it reuses the diff helpers (`childChanged`/`canonicalEqual` from `save-helpers`), the `reference` store, and the change-state tokens/primitives. It re-implements the save orchestration (the generic `persistEntity` is single-catalogue): every cross-path prerequisite change is normally posted together in one combined batch (last, after paths/proficiencies/child collections) so the backend validates the final combined graph and a gateway swap spanning two tiers can't be false-rejected as a transient cycle depending on POST order. The one exception is scoped narrowly: when this save is also retiring a path, the subset of prerequisite changes that touch only already-persisted tiers (no id remap needed) posts *first*, before path identities — otherwise the backend's retire guard would see the still-live edge and reject the retire even though the removal is in the same save. A save that isn't retiring a path always keeps the full batch invariant; only a save that both retires and reverses a persisted edge while touching a brand-new tier can still hit the narrowed edge case. A second, independent exception guards a proficiency's `MaxLevel`: the backend rejects an identity Edit that shrinks it below an already-persisted modifier/reward's level, so lowering the cap and removing that now-out-of-range payout in the same save posts the child-collection removal *before* the proficiency identity edit — scoped to exactly the proficiencies where the removal resolves the violation, so an unresolved shrink still reaches the identity edit unsplit and is correctly rejected. Reach for this shape when one screen must author several related catalogues at once.
