namespace Game.Core.Events
{
    /// <summary>
    /// Marker interface for all domain events.
    /// Domain events represent things that happened within the domain that other parts of the
    /// system may need to react to.
    /// </summary>
    public interface IDomainEvent { }
}
